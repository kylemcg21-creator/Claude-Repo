{
  "name": "blockcraft-guide-site",
  "private": true,
  "type": "module",
  "description": "Test tooling for the root static site (index.html, css/, js/). Not a build step — the site itself remains dependency-free and deploys as-is (see vercel.json).",
  "scripts": {
    "test": "node --test js/main.test.js"
  },
  "devDependencies": {
    "jsdom": "^25.0.0"
  }
}
