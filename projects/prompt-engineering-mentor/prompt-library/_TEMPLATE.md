# [Entry name — short, verb-first if possible]

**Tags:** use-case: `?` · format: `?` · strength: `?`
**Models tested:** [model + date, e.g. Claude Sonnet, 2026-07]
**Situational conditions:** [only if strength is `situational` — what it depends on]

## The prompt

```text
[The exact prompt text. Use {{PLACEHOLDERS}} for the parts that vary per use.]
```

## Placeholders

| Placeholder | What goes there |
|---|---|
| `{{...}}` | ... |

## Why it works

[The load-bearing part. For each non-obvious element of the prompt: what it does and which principle it applies. Someone reading only this section should be able to adapt the prompt to a new situation — that's the test of whether it's complete.]

## Testing evidence

- **Probes:** [what inputs it was tested on, including the edge case]
- **Criteria:** [the written pass/fail criteria it was scored against]
- **Result:** [e.g. "Stable — 3/3 runs passed all criteria on Claude"]

## Known limitations

[Where it breaks or underperforms. An entry with no known limitations hasn't been tested hard enough.]

## Changelog

- YYYY-MM-DD — banked. [origin: which session/task it came from]
